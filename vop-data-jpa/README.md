# VOP :: Data JPA

Reusable JPA helpers.

## Components
- `Specifications` (eq, like, ge/le, between)
- `PageableUtil`
- `SecurityAuditorAware` (uses networkId when available)

## Notes
Does not hard‑require Spring Security; works even if absent.
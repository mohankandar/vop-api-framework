# VOP :: Starter Data

Data basics for JDBC/JPA + Redis.

## Features
- Datasource + Liquibase wiring
- Optional embedded Redis for `local`
- `JdbcTemplate` helper

## Depends On
- Spring Data JPA, Redis (optional)
- `vop-data-jpa`
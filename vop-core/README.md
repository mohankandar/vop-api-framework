# VOP :: Core

Core utilities and abstractions used by all modules.

## Includes
- API envelopes, error models, paging.
- Utilities (masking, time, collections).
- Base exceptions.

## Dependencies
- Minimal (Spring Context / none).
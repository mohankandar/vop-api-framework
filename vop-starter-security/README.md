# VOP :: Starter Security

JWT + API Key auth and outbound token propagation.

## Components
- `ApiKeyAuthFilter`
- `SecurityAutoConfiguration`
- `TokenPropagationConfig` (WebClient/RestTemplate)

## Notes
Requires Spring Security; works with Okta JWT resource server.